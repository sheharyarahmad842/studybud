setup:
 addons:
 - plan: heroku-postgresql
build:
 docker:
  web: Dockerfile
  config:
    DJANGO_SETTINGS_MODULE: studybud_project.settings
run:
 web: gunicorn studybud_project.wsgi
release:
 image: web
 command:
  - python manage.py collectstatic --noinput
