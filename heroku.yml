setup:
 addons:
 - plan: heroku-postgresql
build:
 docker:
  web: Dockerfile
run:
 web: gunicorn studybud_project.wsgi
release:
 image: web
 command:
  - python manage.py collectstatic --noinput
